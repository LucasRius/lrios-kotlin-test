name: Teste

on: [push, workflow_dispatch]
jobs:
    teste:
        uses: LucasRius/lrios-ci-complete/.github/workflows/ci.yml@main
        with:
            language: 'kotlin'
            build_tool: 'gradle'
            lint_bypass: true
            cache_path: |
                **/*.jar
                **/*.tgz
            
          

